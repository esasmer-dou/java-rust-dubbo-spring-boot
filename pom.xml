<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.reactor</groupId>
  <artifactId>java-rust-dubbo-spring-boot-parent</artifactId>
  <version>0.1.1</version>
  <packaging>pom</packaging>

  <name>Java Rust Dubbo Spring Boot</name>
  <description>Reflection-free Spring Boot integration backed by a dedicated Rust Dubbo data plane.</description>
  <url>https://github.com/esasmer-dou/java-rust-dubbo-spring-boot</url>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
      <distribution>repo</distribution>
    </license>
  </licenses>

  <scm>
    <connection>scm:git:git://github.com/esasmer-dou/java-rust-dubbo-spring-boot.git</connection>
    <developerConnection>scm:git:ssh://git@github.com/esasmer-dou/java-rust-dubbo-spring-boot.git</developerConnection>
    <url>https://github.com/esasmer-dou/java-rust-dubbo-spring-boot</url>
    <tag>HEAD</tag>
  </scm>

  <issueManagement>
    <system>GitHub</system>
    <url>https://github.com/esasmer-dou/java-rust-dubbo-spring-boot/issues</url>
  </issueManagement>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/esasmer-dou/java-rust-dubbo-spring-boot</url>
    </repository>
  </distributionManagement>

  <modules>
    <module>java-rust-dubbo-annotations</module>
    <module>java-rust-dubbo-runtime</module>
    <module>java-rust-dubbo-native-windows-x64</module>
    <module>java-rust-dubbo-native-linux-x64</module>
    <module>java-rust-dubbo-codegen</module>
    <module>java-rust-dubbo-enhancer-maven-plugin</module>
    <module>java-rust-dubbo-spring-boot-starter</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <project.build.outputTimestamp>2026-08-28T00:00:00Z</project.build.outputTimestamp>
    <spring-boot.version>3.2.4</spring-boot.version>
    <asm.version>9.8</asm.version>
    <maven.version>3.9.9</maven.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <directory>${maven.multiModuleProjectDirectory}</directory>
        <includes>
          <include>LICENSE</include>
          <include>NOTICE</include>
          <include>THIRD_PARTY_NOTICES</include>
        </includes>
        <targetPath>META-INF</targetPath>
      </resource>
    </resources>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${maven.compiler.release}</release>
            <parameters>true</parameters>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-deploy-plugin</artifactId>
          <version>3.1.3</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <version>3.3.1</version>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals><goal>jar-no-fork</goal></goals>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>runtime-boundary</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[21,22)</version></requireJavaVersion>
                <requireMavenVersion><version>[3.9,)</version></requireMavenVersion>
                <bannedDependencies>
                  <searchTransitive>true</searchTransitive>
                  <excludes>
                    <exclude>org.apache.dubbo:*</exclude>
                    <exclude>io.netty:*</exclude>
                    <exclude>org.apache.zookeeper:*</exclude>
                    <exclude>org.apache.curator:*</exclude>
                    <exclude>com.caucho:hessian</exclude>
                  </excludes>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

</project>
